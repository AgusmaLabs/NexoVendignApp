name: vendingapp
description: VendingApp — NexoVending mobile client for replenishment operators.
publish_to: "none"
version: 0.1.0+1

# Flutter 3.47.3 / Dart 3.13.3 (stable)
environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
